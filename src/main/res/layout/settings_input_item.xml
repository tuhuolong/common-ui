<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/settings_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="33dp"
        android:ellipsize="end"
        android:singleLine="true"
        android:textColor="@color/setting_input_item_default_title_text_color_normal"
        android:textSize="@dimen/setting_input_item_title_text_size"
        tools:text="标题" />

    <EditText
        android:id="@+id/settings_item_input"
        style="@style/Widget.EditText"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:background="@null"
        tools:hint="请输入文案" />

    <ImageView
        android:id="@+id/settings_item_arrow"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical"
        android:paddingLeft="10dp"
        android:paddingRight="20dp"
        android:scaleType="centerInside"
        android:src="@drawable/setting_input_item_arrow_down" />
</LinearLayout>
